# drift — Engine binaries

Pre-built drift engine binaries for macOS (arm64), Linux (x64), and Windows (x64).

**Install:** `pipx install drift-ml` or `npm install -g drift-ml` (requires pipx for CLI), then run `drift`.

Binaries are published automatically from [intent2model](https://github.com/lakshitsachdeva/intent2model) on tag push (e.g. `v0.1.1`).
